# Production origin used by metadata, Open Graph, sitemap and robots.
# Set this on Vercel once the domain is live, e.g. https://your-domain.com
# Until then the app falls back to a local origin and the sitemap
# should not be submitted to search engines.
NEXT_PUBLIC_SITE_URL=
