import type { NextConfig } from "next";

/**
 * Intentionally lean: no plugins, no image domains to declare yet.
 * When project media moves to external hosting, add `images.remotePatterns`
 * here rather than scattering config through components.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
